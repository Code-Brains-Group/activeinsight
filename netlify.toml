[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# Nitro's "netlify" preset emits a server function that handles SSR + API routes.
# It writes the function bundle to .netlify/functions-internal and registers
# the catch-all redirect automatically — no extra [[redirects]] block needed.
